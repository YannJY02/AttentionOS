# Project State

Updated: 2026-07-14 14:15 CST
Status: active

## Documentation Governance Mapping

- Current recovery entrypoint: `docs/governance/project-state.md`.
- Durable decisions: `docs/decisions/`. AI may draft `status: proposed` decisions, but must not mark them accepted without explicit user confirmation.
- Raw or source evidence: `docs/sources-or-raw/`. Existing source-like evidence currently includes `docs/sources-or-raw/user-original-long-prompts.zh-CN.md`; do not move or rewrite it without confirmation.
- Documentation entrypoint: `docs/README.md`.
- Active implementation plans and working notes: `docs/plans/`. Do not recreate root-level `plans/`.
- Working notes: `docs/work/`.
- Archive: `docs/archive/`. Moving key files there requires confirmation.
- AI maintenance surfaces: `docs/governance/`. These files are non-authoritative audit, proposal, and maintenance records.
- Repo-local AI instructions: `AGENTS.md`.
- Repository-specific agent configuration: `AGENTS.md` and `docs/agents/`.
- AI-generated documentation workflow: `docs/governance/ai-generated-doc-workflow.md`.
- Developer intake queue: `docs/work/todo.md`.
- Development document lifecycle: `docs/governance/development-document-lifecycle.md`.
- Documentation automation: `docs/governance/documentation-automation.md` and `scripts/check-doc-governance.mjs`.

## Current Truth

- This repo is AttentionOS V2, a personal attention and workflow/context system. Evidence: `AGENTS.md`, `package.json`, and the product baseline.
- Repository normalization is active under Wayfinder map [#8](https://github.com/YannJY02/AttentionOS/issues/8) on `codex/attentionos-normalization`; commit `b251c57` is the preserved pre-migration rollback baseline.
- The accepted runtime is the local desktop app. Issue #15 removed the disconnected server, Supabase, storage, SDK, MCP, sync, and policy placeholder surfaces; older plans and logs that name them are historical baseline evidence, not current runtime ownership.
- `packages/workflow` is now the owner of canonical workflow types, immutable Workflow Facts, hierarchy validation/mutation rules, and the Ritual/Overview/Execution state machines. Desktop hierarchy storage keeps only local persistence and audit side effects; the temporary `packages/machines` and `packages/core` compatibility surfaces remain until their later retirement ticket.
- The product and vision baseline is `docs/product/MASTER_PRODUCT_PLAN.zh-CN.md`, which declares itself the product/design/development/acceptance baseline.
- The workflow semantic baseline is `docs/workflow/WORKFLOW_CANONICAL_MODEL.zh-CN.md`, which declares the three-stage/five-layer model as active source of truth.
- Original user prompt evidence is categorized under `docs/sources-or-raw/user-original-long-prompts.zh-CN.md`.
- `docs/plans/2026-03-21-attentionos-v2-architecture-design.md` is an architecture draft with status `待审批`; treat it as a working architecture reference, not as accepted authority where it conflicts with current baselines.
- `docs/plans/` is the only active plans directory; the former root `plans/phase1-deterministic-core.md` has been archived at `docs/archive/plans/phase1-deterministic-core.md`.
- Documentation governance surfaces have been consolidated under `docs/`: `docs/governance/project-state.md`, `docs/governance/changelog.md`, `docs/decisions/`, `docs/sources-or-raw/`, `docs/work/`, `docs/archive/`, and `docs/governance/`.
- Top-level `docs/` now keeps only `docs/README.md`; product, workflow, source, governance, plan, app, work, and archive documents live in subdirectories.
- The documentation authority map is accepted in `docs/decisions/2026-05-09-doc-governance-authority-map.md`.
- Future AI-generated documentation must follow `AGENTS.md` and `docs/governance/ai-generated-doc-workflow.md`: classify first, route under `docs/`, standardize naming, update indexes/state/logs, and archive accepted/applied/superseded generated files.
- The developer todo and AI development document lifecycle are accepted in `docs/decisions/2026-05-09-developer-todo-and-doc-lifecycle.md`.
- `docs/work/todo.md` is the only developer todo/intake queue. It is non-authoritative and must not replace project-state, plans, decisions, changelog, or archive.
- Low-risk documentation governance checks are available through `pnpm docs:check`; `pnpm check` runs them before the normal Turbo check.
- This checkout has `core.hooksPath` set to `.githooks`, so commit-time documentation governance checks are active locally.
- Completed tasks and tracked issues with repository changes now require an automatic scoped commit and normal push before completion. Read-only or tracker-only work does not create empty commits, and unsafe isolation or push failure remains an owner-intervention blocker.
- Phase 2 AI reasoning work is represented on `main` by git commit `489b6c6` (`merge: phase 2 ai reasoning`) and by `docs/plans/2026-05-09-phase-2-completion-ledger.md`.
- Phase 3 evolutionary learning now has a verified implementation ledger at `docs/plans/2026-05-09-phase-3-evolutionary-learning-ledger.md`.
- Phase 3 closeout verification passed on 2026-05-09 19:30 CST: `pnpm check`, `pnpm test:run`, `pnpm lint`, `pnpm build`, and `pnpm e2e`.
- Phase 3 was merged and pushed to `origin/main` at commit `ebda370` (`merge: phase 3 evolutionary learning`).
- Phase 4 has a draft intake at `docs/plans/2026-05-09-phase-4-intake.md`; it is not an accepted implementation contract yet.
- Experience alignment now has a draft blueprint at `docs/plans/2026-05-10-attentionos-experience-alignment-blueprint.md`; it requires exact `/gstack` skill gates before implementation and does not authorize code changes by itself.
- Phase 0 `/gstack-investigate` evidence freeze for experience alignment is complete at `docs/plans/2026-05-10-attentionos-experience-alignment-evidence-ledger.md`; it identifies the root cause as a missing product-to-frontend experience contract plus a concrete fixed-shell mobile layout bug.
- Phase 1 `/gstack-design-consultation` experience contract is complete at `docs/plans/2026-05-10-attentionos-experience-alignment-contract.md`.
- Phase 2 `/gstack-plan-design-review` is complete under owner-approved normal-chat fallback, with D1-D5 recorded in `docs/plans/2026-05-10-attentionos-experience-alignment-contract.md`: mobile bottom stage navigation with top layer context, `/execution/plan` plus `/execution/focus` subroutes, neutral default Ritual language with configurable prayer/dedication, timeline-first Vision, and AI suggestions grouped by workflow stage/current mode.
- Phase 3 `/gstack-plan-eng-review` is complete under owner-approved normal-chat fallback, with E1-E5 recorded in `docs/plans/2026-05-10-attentionos-experience-alignment-contract.md`: add `executionModeMachine`, let URL request and machine validate mode, keep task lifecycle separate with a migration path, require the full contract test set, and implement as one complete slice.
- The experience-alignment implementation slice is implemented and QA-verified as of 2026-05-13 08:09 CST. The completed slice covers `executionModeMachine`, `/execution/plan`, `/execution/focus`, mobile bottom stage navigation, Plan/Focus AI boundaries, active task lifecycle preservation across Plan/Focus, and contract tests.
- `/gstack-qa-only` produced `.gstack/qa-reports/qa-report-127-0-0-1-1420-2026-05-13.md` and `.gstack/projects/YannJY02-AttentionOS/yann.jy-main-test-outcome-20260513-0749.md`; no QA defects were found.
- Final verification passed on 2026-05-13 08:09 CST: `pnpm docs:check`, `pnpm check`, `pnpm test:run`, `pnpm lint`, `pnpm build`, `pnpm e2e`, and `git diff --check`.
- Phase 6 `/gstack-context-save` has saved the current experience-alignment handoff at `.gstack/projects/YannJY02-AttentionOS/checkpoints/20260510-220217-attentionos-experience-alignment-gates.md`; `/gstack-context-restore` is only needed in a future resume context.
- Ritual semantic correction Slice 2 has an implementation ledger at `docs/plans/2026-05-13-attentionos-ritual-semantic-correction-slice.md`.
- Ritual semantic correction is implemented and `/gstack-qa-only` verified on branch `codex-experience-ritual-semantics`: Ritual now uses intention-led neutral copy, configurable local intention/dedication wording, user-facing breath labels, and attention-first shell footer copy while preserving the existing Ritual -> Overview flow.
- `/gstack-qa-only` produced `.gstack/qa-reports/qa-report-ritual-semantics-2026-05-13.md`; no QA defects, console errors, or page errors were found in the final Ritual QA pass.
- Final Ritual slice verification passed on 2026-05-13 09:10 CST: `pnpm docs:check`, `pnpm check`, `pnpm test:run` with 200 tests, `pnpm lint`, `pnpm build`, `pnpm e2e` with 3 Chromium tests, and `git diff --check`.
- Overview read-only scan Slice 3 has an implementation ledger at `docs/plans/2026-05-13-attentionos-overview-readonly-scan-slice.md`.
- Overview read-only scan is implemented and `/gstack-qa-only` verified on branch `codex-experience-ritual-semantics`: Overview now leads with layer scan context, renders a timeline-first Vision panel before learning metrics, preserves read-only controls, and keeps the task-layer bridge into Execution.
- `/gstack-qa-only` produced `.gstack/qa-reports/qa-report-overview-readonly-scan-2026-05-13.md`; no QA defects, console errors, or page errors were found in the Overview QA pass.
- Final Overview slice verification passed on 2026-05-13 09:31 CST: `pnpm docs:check`, `pnpm check`, `pnpm test:run` with 200 tests, `pnpm lint`, `pnpm build`, `pnpm e2e` with 3 Chromium tests, and `git diff --check`.
- AI HITL clarity Slice 5 has an implementation ledger at `docs/plans/2026-05-13-attentionos-ai-hitl-clarity-slice.md`.
- AI HITL clarity is implemented and `/gstack-qa-only` verified on branch `codex-experience-ritual-semantics`: Execution Plan now groups suggestions under human review lanes, task decomposition supports explicit rejection without creating tasks, workflow optimization review copy no longer implies automatic mutation, and Focus mode keeps broad AI queues hidden.
- `/gstack-qa-only` produced `.gstack/qa-reports/qa-report-ai-hitl-clarity-2026-05-13.md`; no QA defects, console errors, or page errors were found in the AI HITL QA pass.
- Final AI HITL slice verification passed on 2026-05-13 09:48 CST: `pnpm docs:check`, `pnpm check`, `pnpm test:run` with 206 tests, `pnpm lint`, `pnpm build`, `pnpm e2e` with 3 Chromium tests, and `git diff --check`.
- Integration polish and regression Slice 6 has an implementation ledger at `docs/plans/2026-05-13-attentionos-integration-polish-regression-slice.md`.
- Integration polish and regression is implemented and `/gstack-qa-only` verified on branch `codex-experience-ritual-semantics`: no tracked application-code changes were required, the primary Ritual -> Overview -> Execution flow passed, Overview remained read-only, Focus hid broad review lanes, and no old scaffold copy was found in the primary flow.
- `/gstack-qa-only` produced `.gstack/qa-reports/qa-report-integration-regression-2026-05-13.md`; no QA defects, console errors, or page errors were found in the final integration QA pass.
- Final integration slice verification passed on 2026-05-13 09:57 CST: `pnpm docs:check`, `pnpm check`, `pnpm test:run` with 206 tests, `pnpm lint`, `pnpm build`, `pnpm e2e` with 3 Chromium tests, and `git diff --check`.
- Experience alignment was fast-forward merged into local `main` at `faee10d` on 2026-05-13 11:41 CST and pushed to `origin/main` through `8ff2d7f` on 2026-05-13 11:45 CST.
- Final `/gstack-context-save` closeout checkpoint was saved at `.gstack/projects/YannJY02-AttentionOS/checkpoints/20260513-113528-final-experience-alignment-branch-closeout.md`.
- Matt Pocock skills are supplied by the owner's global configuration; this repository no longer vendors copies under `.agents/skills/` or a `skills-lock.json`.
- Project-local gstack runtime and generated skill surfaces remain local tooling artifacts; `.gstack/` and `.agents/skills/gstack*` stay ignored rather than becoming durable repo configuration.
- Repository-specific agent support docs live under `docs/agents/`: issue tracker routing, triage labels, and domain-context pointers.
- Release-candidate coverage work is active at `docs/plans/2026-05-23-attentionos-release-candidate-requirements-ledger.md`. It maps `docs/product/MASTER_PRODUCT_PLAN.zh-CN.md`, `docs/workflow/WORKFLOW_CANONICAL_MODEL.zh-CN.md`, and `docs/sources-or-raw/user-original-long-prompts.zh-CN.md` to implementation and verification status. The ledger is not a completion claim; critical `Open` items remain.
- The first release-candidate implementation slice added native/browser app-state snapshots, backup/export/import controls, and a Data & Settings route. It is verified for the local web surface and Rust compile path, but it does not yet close full packaged macOS persistence, corrupted-state recovery, privacy, accessibility, or release packaging.
- The second release-candidate implementation slice added Settings controls for ritual intention/prayer, dedication/回向, meditation duration, guidance mode, and sound cue preference. Ritual now renders those settings and includes them in portable snapshots, but schedule behavior, mark-to-task/project behavior, native timing/sound QA, reminders, privacy controls, accessibility, and release packaging remain open.
- The third release-candidate implementation slice added Ritual reflection follow-up markers for task/project input and an Execution Plan lane that surfaces those marked ritual inputs without auto-creating tasks or projects. Dedication-content follow-up handling and an explicit create/review pipeline remain open.
- The fourth release-candidate implementation slice added human-created Execution Plan tasks/projects with clarification, project-signal validation, one-current/three-candidate planning roles, persisted focus-candidate selection, and audit entries. AI pre-split clarification, richer prioritization, existing-entity editing, native restart proof, and release packaging remain open.
- The fifth release-candidate implementation slice added a required human clarification gate for AI task decomposition and approval-time validation for generated steps. Broader atomicity validation, imported/existing task validation, native restart proof, privacy/a11y, and release packaging remain open.
- The sixth release-candidate implementation slice hardened Execution Plan validation: task creation now requires a start note, rejects over-two-hour estimates instead of silently clamping them, revalidates AI-generated steps at the persistence boundary, and blocks project nesting deeper than one subproject layer. Import validation, existing-entity editing, native restart proof, privacy/a11y, and release packaging remain open.
- The seventh release-candidate implementation slice added Ritual cadence settings with default morning/evening times, user-editable morning/evening times, manual-entry preference, schedule display in Ritual, dedication carry-forward markers, and an Execution Plan draft bridge from marked Ritual inputs. Reminder notifications, native launch/fullscreen behavior, native restart proof, privacy/a11y, and release packaging remain open.
- The eighth release-candidate implementation slice hardened the Tauri macOS shell from template state to AttentionOS product identity: `productName`/window title are `AttentionOS`, bundle id is `com.yannjy.attentionos`, CSP is no longer null, the unused opener permission/plugin was removed, and unsigned local `.app` plus `.dmg` bundles build successfully. Computer Use verified the packaged app renders and advances Ritual through Meditation, Reflection, and Dedication. Developer signing, notarization, App Store upload, and certificate/provisioning work remain owner-credential blocked.
- The ninth release-candidate implementation slice added corrupt snapshot quarantine and recovery visibility: native `attentionos-state.json` can be moved into an app-data `recovery/` copy, browser corrupt snapshots are preserved, import/restore rejects unknown or malformed managed entries before mutation, and Shell plus Data & Settings now show a non-destructive recovery warning. Computer Use packaged-app QA then verified native app-data restore into Overview and native corrupt-state quarantine/recovery details, with local app/WebKit state restored afterward. Packaged Settings backup/import QA, privacy/a11y, onboarding, performance, and final release readiness remain open.
- The tenth release-candidate implementation slice added local-first Privacy & AI settings: local-data acknowledgement, future external AI call consent default-off, telemetry opt-in default-off, and persistence of those preferences into portable snapshots. The controls do not create credentials, do not call providers, and do not activate telemetry. Security/privacy review, secret scan, permissions review, user-facing policy text, reminder consent, a11y, onboarding, and performance evidence remain open.
- The first bounded release-candidate security/privacy scan found no high-confidence literal provider keys in current source/config, found Supabase service-role usage only through `requireEnv` and non-leak tests, found no `fetch`/WebSocket/EventSource network primitives in app/package source, found no telemetry vendor/sender usage, and confirmed the Tauri capability remains `core:default`. Broader privacy policy text, permissions UX, reminder consent, a11y, onboarding, and performance evidence remain open.
- The eleventh release-candidate implementation slice added first-run onboarding: new users route from `/` to `/onboarding`, the route explains Ritual -> Overview -> Execution, local-first data, external AI/telemetry/cloud-sync default-off posture, and the non-medical boundary, and completion persists onboarding plus local-first privacy state into portable snapshots. Final Browser/native QA, accessibility, reminder consent, performance, and release readiness remain open.
- The twelfth release-candidate implementation slice added local reminder consent settings: reminders stay disabled by default, Settings stores frequency, quiet-hours start/end, and priority override intent, and copy states that no macOS notification permission is requested and no background notifications are scheduled in this build. Native notification permission/scheduling remains a future owner-approved behavior decision.
- The thirteenth release-candidate implementation slice added a route-level Playwright accessibility audit for onboarding, Ritual, Overview, Execution Plan, seeded Execution Focus, and Settings, and fixed the hidden Settings JSON import input so it has an accessible name. This closes a basic semantic regression gap but not full release-grade accessibility: VoiceOver, native packaged app accessibility, contrast, reduced motion, and full manual keyboard traversal remain open.
- The fourteenth release-candidate implementation slice added browser performance/usability smoke coverage for first route readiness, onboarding to Ritual, Ritual to Overview, stage navigation, Execution Plan to Focus, Focus controls, Settings snapshot feedback, and corrupt browser snapshot recovery. Native packaged launch/navigation/recovery timing and manual usability review remain open.
- The fifteenth release-candidate QA slice used Computer Use against the packaged `AttentionOS.app` to verify native Settings save snapshot, native backup creation, native JSON import through the file picker, and imported Ritual rendering. Existing local `com.yannjy.attentionos` app-data/WebKit/cache state was preserved before QA and restored afterward. Backup rotation/export guidance and native Execution Plan/Focus QA remain open.
- The sixteenth release-candidate QA slice used Computer Use against the packaged `AttentionOS.app` to verify the native Overview five-layer bridge into Execution Plan, selected focus candidate, Execution Focus planning/executing/reviewing/completion states, +5 minute progress, and return to Overview. Existing local app state was again preserved and restored. Native restart recovery, manual/native accessibility, and native performance remain open.
- The seventeenth release-candidate implementation/QA slice added persisted active Focus runtime under `attentionos.execution.taskRuntime.v1`, restored active task state on Focus mount, extended E2E coverage so an executing +5-minute Focus state survives reload, rebuilt the unsigned packaged app, and used Computer Use to verify packaged native Focus recovery after process restart. Manual/native accessibility, explicit exit-reason UX, native performance, and signing/notarization remain open.
- The eighteenth release-candidate implementation slice made Focus exit explicit: `Pause and exit focus` pauses executing/reviewing work, persists paused progress, audits the transition, and returns to Execution Plan; `Exit focus` leaves planning/paused Focus without mutating state. Browser E2E verifies exit-to-plan and re-entry with paused 5-minute progress. Native performance, manual/native accessibility, final cross-flow packaged QA, and signing/notarization remain open.
- The nineteenth release-candidate evidence pass audited the existing workflow surface and closed ledger rows for Personal Attention OS framing, workflow-stage organization, Overview read-only behavior, current-layer/breadcrumb context, and the Overview-to-Execution bridge. This was based on current source, Overview tests, browser E2E, and packaged native five-layer QA; richer task overview modes, metrics, native accessibility, and native performance remain open.
- The twentieth release-candidate regression slice added storage coverage for competing Execution Plan current actions: selecting a second current sibling task updates the single focus candidate, demotes the previous current action, and clearing/moving the selected action removes the focus candidate. Final packaged cross-flow QA should still recheck user-created competing tasks, but the core RC-030 invariant is now verified.
- The twenty-first release-candidate evidence pass closed RC-005 for current local AI surfaces: task decomposition requires clarification, remains pending until explicit approval, rejection creates no tasks, invalid/wrong-target/non-pending suggestions fail before hierarchy mutation, and workflow optimization review does not automatically change the workflow. RC-006 remains open for broader rollback/compensation after applied suggestions.
- The twenty-second release-candidate regression slice closed RC-012 for current routes: `/execution/plan` and `/execution/focus` are the active execution-mode surfaces, `/execution` redirects to Plan, route sync maps Focus under the `execution` stage, and `/planning` plus `/focus` are tested as non-active legacy routes.
- The twenty-third release-candidate evidence pass closed RC-008 for current active copy: the app requires non-medical acknowledgement during onboarding, states AttentionOS is not medical advice/diagnosis/treatment and does not diagnose or treat ADHD/anxiety/depression/health conditions, and a bounded source scan found no active diagnosis/treatment claims outside that boundary copy. Final legal/privacy review remains separate.
- The twenty-fourth release-candidate privacy/settings audit closed RC-039, RC-046, and RC-052 for current local implementation: Settings covers ritual, meditation, privacy/AI, telemetry, reminder intent, backup/export/import, and native Settings save/backup/import QA; scans show no active provider keys, telemetry sender, network primitive, or broad Tauri permission; no passive sensing or notification scheduling is active. Native notification delivery remains separately tracked under RC-042, and final legal/App Store privacy review remains owner-controlled.
- The twenty-fifth release-candidate data-control slice closed RC-047 for current local release scope: native backups and portable exports are now separate Tauri commands/paths, browser/dev export remains a non-mutating fallback, Settings reports export paths when available, and the Data & Settings route shows snapshot/backup/export/restore rotation and location boundaries. Broader verification passed afterward with `cargo check`, full `pnpm test:run` with 253 tests, `pnpm build`, `pnpm check`, `pnpm e2e` with 14 Chromium tests, unsigned `.app` packaging, and package metadata/ad hoc signing inspection. Final packaged QA should recheck native export after remaining storage changes.
- The twenty-sixth release-candidate Ritual runtime slice closed RC-014 and RC-015 for current local release scope: the meditation machine now clamps elapsed time to the configured duration and auto-completes at the timer boundary, Ritual advances to Reflection from the completed machine state, tests cover timer completion and bell cue invocation, and Computer Use verified the rebuilt packaged app with configured 1-minute target, body-scan guidance, opening/closing bell label, start, elapsed progress, pause stability, resume, and automatic transition to Reflection. Full verification passed afterward with docs, lint, diff check, cargo, 256 tests, build, check, 14 E2E tests, unsigned `.app` packaging, and bundle metadata/ad hoc signing inspection. Audio capture was not performed; fullscreen/scheduled launch and notification delivery remain separate rows.
- The twenty-seventh release-candidate Reflection slice closed RC-016 for current local release scope: malformed reflection storage is quarantined and surfaced before accepting a new note, corrupt payloads are preserved, Reflection has a visible empty state while Save is disabled, valid reflections still persist as Ritual entities, and portable snapshot export/import restores valid reflection data. Full verification passed afterward with docs, lint, diff check, cargo, 263 tests, build, check, 14 E2E tests, unsigned `.app` packaging, and bundle metadata/ad hoc signing inspection. Broader workflow-level error states remain tracked under RC-050.
- The twenty-eighth release-candidate hierarchy validation slice closed RC-033 for current local scope: imported hierarchy snapshots are rejected before mutation when they violate task/project classification, parent progression, one-subproject-layer limits, task duration bounds, one-current/three-candidate role limits, or execution-role vocabulary; existing local role edits also validate hierarchy invariants before saving while preserving compatibility for older partial ancestry. Full verification passed afterward with docs, lint, diff check, cargo, 266 tests, build, check, 14 E2E tests, unsigned `.app` packaging, and bundle metadata/ad hoc signing inspection.
- The twenty-ninth release-candidate Execution Plan ordering slice closed RC-028 for current local scope: storage coverage now verifies project actions sort as current action, candidates by creation age, then later-list/backlog actions; it also verifies current next-action limits are project-specific across persisted reads while the global focus candidate follows the most recently selected current action. Full verification passed afterward with docs, lint, diff check, cargo, 268 tests, build, check, 14 E2E tests, unsigned `.app` packaging, and bundle metadata/ad hoc signing inspection.
- The thirtieth release-candidate Overview signal slice closed RC-023 for current local scope: Overview now shows a read-only risk/trend/context signal panel between the Vision timeline and Learning snapshot, deriving risk from local attention and task-size evidence, trend from attention/completion metrics, and context from visible hierarchy count, marked Ritual follow-up inputs, and pending AI optimization count. Full verification passed afterward with docs, lint, diff check, cargo, 269 tests, build, check, 14 E2E tests, unsigned `.app` packaging, and bundle metadata/ad hoc signing inspection.
- The thirty-first release-candidate Task Overview slice closed RC-035 and RC-036 for current local scope: the task layer now shows a read-only browsing panel with Day/Week/Month mode controls, grouping by parent project, and four-quadrant lanes for Do next, Schedule, Shrink or delegate, and Later list. Full verification passed afterward with docs, lint, diff check, cargo, 270 tests, build, check, 14 E2E tests, unsigned `.app` packaging, and bundle metadata/ad hoc signing inspection.
- The thirty-second release-candidate five-layer semantics evidence slice closed RC-011 for current local scope: Overview tests now load custom persisted Vision -> Area -> Goal -> Project -> Task data and verify layer-specific copy, breadcrumb navigation, Vision timeline behavior, Task Overview browsing, and the task execution bridge beyond default seed examples. Full verification passed afterward with docs, lint, diff check, cargo, 271 tests, build, check, 14 E2E tests, unsigned `.app` packaging, and bundle metadata/ad hoc signing inspection.
- The thirty-third release-candidate empty-state slice closed RC-049 for current primary routes: Overview now has layer-specific empty states, Execution Plan shows no Ritual-input and no planned-action guidance, existing Reflection and Focus empty states remain covered, and tests verify the main empty-state paths. Full verification passed afterward with docs, lint, diff check, cargo, 272 tests, build, check, 14 E2E tests, unsigned `.app` packaging, and bundle metadata/ad hoc signing inspection.
- The thirty-fourth release-candidate storage-recovery slice closed RC-050 for current local error-state scope: malformed helper-storage reads now preserve original payloads in a storage-recovery ledger, fall back safely, and surface global Shell plus detailed Settings warnings with clear actions; hierarchy corruption is preserved before default seed data is written back. Full verification passed afterward with docs, lint, diff check, cargo, 276 tests, build, check, 14 E2E tests, unsigned `.app` packaging, and bundle metadata/ad hoc signing inspection.
- The thirty-fifth release-candidate metrics slice closed RC-043 and RC-044 for current local scope: Overview now exposes read-only release metrics and guardrail metrics derived from local attention observations, execution audit entries, hierarchy tasks, AI suggestions, reflection inputs, and reminder settings, covering attention ratio, switching pressure, recovery cues, focus success, plan fulfillment, reminder load, recording friction, autonomy kept, and misjudgment signal without enabling passive sensing or telemetry. Full verification passed afterward with docs, lint, diff check, cargo, 278 tests, build, check, 14 E2E tests, unsigned `.app` packaging, and bundle metadata/ad hoc signing inspection.
- The thirty-sixth release-candidate interruption-recovery slice closed RC-041 for current local scope: Overview now shows a non-mutating recovery bridge when saved Focus runtime exists, and Execution Plan shows saved minutes/state plus an explicit Continue in Focus action after intentional exit or return from Overview, without adding planning noise to Focus. Full verification passed afterward with docs, lint, diff check, cargo, 279 tests, build, check, 14 E2E tests, unsigned `.app` packaging, and bundle metadata/ad hoc signing inspection.
- The thirty-seventh release-candidate context-capture slice closed RC-037 for current local scope: Capture now stores one local note with idea/task/project/calendar channels, optional calendar context, recent-capture visibility, snapshot backup/export/import coverage, and malformed capture-storage recovery. Execution Plan surfaces task/project captures as draft inputs that prefill the human create form without auto-creating hierarchy entities. Full verification passed afterward with docs, lint, diff check, cargo, 283 tests, build, check, 14 E2E tests, unsigned `.app` packaging, and bundle metadata/ad hoc signing inspection.
- The thirty-eighth release-candidate AI rollback slice closed RC-006 for current local scope: applied AI task-decomposition suggestions now expose a human-triggered rollback/restore path in Execution Plan. Rollback archives only active AI-created tasks from the applied suggestion, preserves original task snapshots in audit, clears an archived focus candidate, and restore reactivates tasks from the audit snapshot with its own audit entry. Workflow-optimization suggestions remain reviewed-only and non-mutating. Full verification passed afterward with docs, lint, diff check, cargo, 284 tests, build, check, 14 E2E tests, unsigned `.app` packaging, and bundle metadata/ad hoc signing inspection.
- The thirty-ninth release-candidate attention-calibration slice closed RC-040 for current local scope: Settings now has a manual attention calibration surface that combines subjective ratings, manual behavior signals, and probe fields into confidence-scored local observations with subjective/passive/behavioral breakdowns. Users can correct the estimated state, and corrections persist in local learning observations plus app-state snapshots. Full verification passed afterward with docs, lint, diff check, cargo, 287 tests, build, check, 14 E2E tests, unsigned `.app` packaging, and bundle metadata/ad hoc signing inspection.
- The fortieth release-candidate attention-first evidence pass closed RC-003 for current local scope: the current release-critical screens now have explicit evidence that they reduce switching cost or improve focus stability. Onboarding routes into the workflow, Ritual settles attention, Overview stays read-only, Execution Plan keeps planning/AI/capture lanes human-reviewed, Focus protects one action, Capture reduces context switching, and Settings keeps recovery/privacy/calibration controls outside Focus. This was an evidence-only documentation pass based on current source plus the final post-RC-040 E2E/performance evidence.
- The forty-first release-candidate integration-boundary slice closed RC-038 for current local scope: Settings now states that the current build does not monitor apps, screens, messages, browser activity, or calendar data in the background, and that future plugin/API/auto-open behavior must declare permissions, stay scoped, user-approved, and auditable. This did not start Phase 4 protocol work; SDK/MCP remain placeholders and the Phase 4 intake remains draft. Full verification passed afterward with docs, lint, diff check, cargo, 288 tests, build, check, 14 E2E tests, unsigned `.app` packaging, and bundle metadata/ad hoc signing inspection.
- The forty-second release-candidate local-first storage evidence pass closed RC-004 for current local scope: all 14 primary desktop product storage keys are covered by the app-state snapshot, Tauri mirrors snapshots into app-local native JSON with backup/export/quarantine commands, startup waits for persistence restore before route content reads defaults, and bounded scans found no active desktop cloud-storage import, sync runtime, network primitive, telemetry sender, or analytics sender. Runtime helpers still use localStorage as the WebView working cache, but cloud is not the primary store for the current release path.
- The forty-third release-candidate accessibility automation slice improved RC-051 but did not close it: global focus-visible and reduced-motion CSS boundaries were added, Playwright accessibility coverage now includes visible text contrast, keyboard-only completion of the primary workflow, and reduced-motion checks, and full verification passed with 16 E2E tests plus an unsigned `.app` rebuild. Native packaged accessibility remains open because Computer Use ran while the macOS session was at the login/lock screen and could not obtain a packaged app window (`cgWindowNotFound`).
- The forty-fourth release-candidate protocol-readiness slice closed RC-007 for current release scope: `@attentionos/sdk` now exposes a stable `attentionos.read.v1` local read contract, and `@attentionos/mcp` maps that same contract to a read-only MCP manifest with no tools. Focused package tests and typechecks verify the SDK/MCP seam is local, user-approved, and read-only, and reject accidental write-tool exposure. Full verification passed afterward with docs, diff check, cargo, 294 tests, build, check, 16 E2E tests, and unsigned `.app` packaging. This remains a contract seam only; broader Phase 4 MCP runtime, client, write tools, plugin loading, auth, offline sync, external app operation, and desktop protocol refactor remain outside this release slice.
- The forty-fifth release-candidate macOS package-readiness slice closed RC-055 for local package metadata and auditability: Tauri now declares App Store category `Productivity`, explicit hardened runtime intent, a macOS Info.plist merge file, and an App Sandbox entitlements file. Full verification passed with docs, lint, diff check, cargo, 294 tests, build, check, 16 E2E tests, unsigned `.app`/`.dmg` packaging, and `pnpm release:check:macos` with 27 checks, 0 failures, and 4 credential-bound external blockers for distribution signing, strict signature verification, signed entitlements, and notarization/App Store upload. RC-056 remains external-blocked until the owner provides Apple Developer signing/profile/notary/App Store credentials and explicit upload/submission authorization.
- The forty-sixth release-candidate native performance slice improved RC-059 but did not close it: an opt-in packaged-app readiness marker and `pnpm performance:check:macos` now measure native post-persistence readiness from a valid native snapshot and corrupt native snapshot recovery while isolating WebKit/cache state. The freshly rebuilt packaged app passed with 1533 ms valid-snapshot readiness and 509 ms corrupt-recovery readiness after full affected verification passed with docs, lint, diff check, cargo, 294 tests, build, check, 16 E2E tests, unsigned `.app`/`.dmg` packaging, and release/package audits. Manual/native usability, GUI-observed navigation timing, VoiceOver, and final cross-flow Computer Use QA remain open until the owner provides an unlocked macOS GUI session.
- The forty-seventh release-candidate browser QA slice improved RC-058 but did not close it: `pnpm qa:browser:release` now builds the desktop production bundle, serves it through Vite preview on `127.0.0.1:1422`, drives onboarding -> Ritual -> Overview -> Execution Plan -> Execution Focus -> completion -> Settings -> mobile navigation in Chromium, and writes screenshot plus console evidence under `.gstack/qa-reports/release-candidate-browser-workflow-2026-05-24-04-00-25/`. The latest run captured 11 screenshots with 0 console errors and 0 page errors. Browser plugin navigation tools were still unavailable after tool discovery, and a current Computer Use attempt against `AttentionOS` still returned `cgWindowNotFound`, so native GUI/VoiceOver evidence remains open.
- The forty-eighth release-candidate owner-unblock slice added `docs/plans/2026-05-24-attentionos-release-candidate-owner-decision-packet.md` as a draft decision packet, not an accepted decision. It narrows the remaining owner-controlled gates for RC-020 fullscreen/scheduled Ritual behavior, RC-042 native notifications, RC-051/RC-058/RC-059 native GUI and VoiceOver evidence, and RC-056 Apple distribution credentials. The active goal still cannot be completed until the owner confirms choices and the ledger reaches terminal statuses.
- The forty-ninth release-candidate ledger-guard slice added `scripts/check-release-candidate-ledger.mjs` plus `pnpm release:ledger` and `pnpm release:ledger:complete`. The normal ledger check passes and reports 60 rows total, with 52 implemented, 7 open, and 1 external blocker. Completion mode intentionally fails on RC-020, RC-042, RC-051, RC-057, RC-058, RC-059, and RC-060, preventing a false release-completion claim while owner-controlled rows remain open.
- The fiftieth release-candidate blocked audit confirms the remaining work is owner-controlled in the current environment: `pnpm release:ledger` still reports 7 open rows and 1 external blocker, no owner decision reply has been recorded, and a fresh packaged-app Computer Use probe after launching `AttentionOS.app` still returns `cgWindowNotFound`. No remaining row can be made terminal without owner choices, Apple credentials, or an unlocked/native-verifiable GUI session.
- The fifty-first release-candidate owner-decision recording slice records the owner reply `D1=A, D2=C, D3=A, D4=B`. RC-020 is now owner-deferred for this release candidate. RC-042 remains open because D2=C expands the scope to broader integration reminders and requires a narrowed implementation/verification slice. D3=A authorizes another Computer Use native GUI/VoiceOver/manual usability pass if the macOS GUI is unlocked or exposed. D4=B selects the Developer ID path, but certificates/profiles/notary credentials and any upload/submission authorization remain external owner-controlled boundaries.
- The fifty-second release-candidate post-decision implementation/QA slice closes RC-042, RC-051, RC-058, and RC-059 for current local RC scope. D2=C is implemented as a local integration-reminder handoff contract with calendar-file, Focus handoff, and app auto-open targets, explicit permission copy, local audit entries, and a 1-6/day fatigue cap. Computer Use now reaches the packaged native app and verified onboarding -> Ritual -> Overview five-layer traversal -> Execution Plan -> Execution Focus progress/review/completion -> Overview return with AX tree evidence. Developer ID prerequisite audit found Apple Development identities only, no Developer ID Application identity, no Apple/Tauri/CSC/notary credential environment variables, and an ad hoc/linker-signed bundle; `xcrun notarytool` is available but signing/notarization remains credential-blocked.
- The fifty-third release-candidate final verification/report slice closes RC-057 and RC-060. Final verification passed with docs, release ledger, diff check, lint, cargo check, 59 Vitest files / 295 tests, Turbo build, workspace check, 16 Playwright E2E tests, unsigned `.app`/`.dmg` rebuild, macOS release audit, native performance timing, and production browser QA. `docs/plans/2026-05-24-attentionos-release-readiness-report.md` is now the final local macOS RC report, with RC-020 owner-deferred and RC-056 externally blocked by Developer ID credentials/release authorization.

## Active Work

- Follow the native dependency chain in Wayfinder map #8. The next unblocked slice is #17, which moves attention estimation, suggestions, metrics, and reminder policy into `packages/guidance` without changing product behavior.
- Keep all project documentation discoverable through `docs/README.md` and prevent new root-level documentation sprawl.
- Keep `docs/governance/proposed-updates/` as a pending-only queue; archive applied proposals under `docs/archive/governance/proposed-updates/`.
- Keep `docs/work/todo.md` short and route confirmed work into the proper authority surface.
- For large AI-assisted tasks, use `docs/governance/development-document-lifecycle.md` to close out prompt, blueprint, verification, audit, and archive artifacts.
- Run `pnpm docs:check` after any documentation-related AI work; commit-time enforcement is active in this checkout via `.githooks/pre-commit`.
- Keep the experience-alignment blueprint and slice ledgers as closeout evidence for the merged frontend/product-experience work.
- Do not expand the read-only release-candidate protocol seam into broad Phase 4 runtime, MCP client/server operations, write tools, plugin loading, auth, external app operation, or offline-sync work without a separate accepted contract.
- Keep shared workflow rules in `AGENTS.md` and `docs/agents/`; do not commit user-level Matt skills or ignored local gstack artifacts.
- Maintain the release-candidate requirements ledger as the controlling evidence surface for the active release goal. The current ledger has no `Open` rows; RC-020 is owner-deferred and RC-056 is external-blocked.
- Use `docs/plans/2026-05-24-attentionos-release-candidate-owner-decision-packet.md` as the current owner-reply record for D1=A, D2=C, D3=A, and D4=B; do not treat it as authorization for uploads, paid services, public release, or persistent credential creation.
- Use `pnpm release:ledger` as the machine-readable current ledger audit and `pnpm release:ledger:complete` as the completion guard before any final readiness claim.

## Blockers

- Older Sprint E/F/G/H roadmap source files are still not present; `docs/roadmap-execution/README.md` now records this instead of leaving broken direct links.
- Experience-alignment implementation, `/gstack-qa-only`, final workspace verification, local `main` merge, and `origin/main` push are complete.
- Phase 4 runtime scope and security decisions are not finalized. D2=C is now closed only as a local integration-reminder handoff contract; do not expand it into a background MCP server/client runtime, plugin loading, Calendar/EventKit writes, macOS Focus automation, opener/shell permissions, external-app control, or offline sync without a separate accepted contract.
- macOS local build/package readiness is now verified through `pnpm release:check:macos`. The owner selected the Developer ID path under D4=B, but distribution signing, signed entitlements, notarization, paid accounts, certificates/profiles, credentials, final legal/privacy confirmation, and any external upload/submission actions still require owner-provided credentials and separate authorization.
- Final native GUI/VoiceOver/manual usability evidence is now recorded through Computer Use AX-tree/native workflow evidence; re-run it if native packaging, route structure, or accessibility-sensitive UI changes.
- The local unsigned release candidate is complete against the current ledger. RC-056 remains external-blocked until the owner provides Developer ID certificate/profile/notarization credentials and separately authorizes signing/notarization; upload/submission remains forbidden without explicit owner authorization.

## Next Action

Execute Wayfinder ticket #17: establish the Attention Guidance context package and migrate existing Guidance ownership with targeted regression checks.
